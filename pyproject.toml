[project]
name = "neaty"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "google-adk>=2.3.0",
    "python-dotenv",
    "pydantic",
]

[project.optional-dependencies]
lint = [
    "ruff",
    "codespell",
]
